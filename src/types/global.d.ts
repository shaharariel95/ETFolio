declare global {
    // eslint-disable-next-line no-var, @typescript-eslint/no-explicit-any
    var mongoose: any; // This must be declared as var to match global property declarations
}

export { };
